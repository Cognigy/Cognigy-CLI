/* Custom Modules*/
import CognigyClient from "../../utils/cognigyClient";

export const deleteDocument = (
	data: {
		knowledgeStoreId: string,
		documentUrl: string,
		verbose?: boolean
	}) => {

	}